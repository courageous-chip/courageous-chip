type Exercise {
  id: ID!
  name: String!
}

type Habit {
  id: ID!
  name: String!
}

type Query {
  exercise(id: ID!): Exercise!
  exercises: [Exercise!]!
  habit(id: ID!): Habit!
  habits: [Habit!]!
}
